# Beeliv-Admin

Serves `admin.beeliv.co` — the Assigned HR and Head of Operations dashboards.

Standard Next.js app. No environment variables needed — no database or authentication is wired up yet (Stage 1 prototype: every form field is intentionally disabled, no data is persisted).
